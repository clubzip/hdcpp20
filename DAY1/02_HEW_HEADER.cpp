// C++20 에서 추가된 헤더
#include <concepts>			// Fundamental library concepts
#include <coroutine>		// Coroutine support library
#include <compare>			// Three - way comparison operator support
#include <version>			// Supplies implementation - dependent library information
#include <format>			// Formatting library including std::format
#include <span>				// std::span view
#include <ranges>			// Range access, primitives, requirements, utilitiesand adaptors
#include <bit>				// Bit manipulation functions
#include <numbers>			// Math constants
#include <syncstream>		// std::basic_osyncstream, std::basic_syncbuf, and typedefs
#include <stop_token>		// Stop tokens for std::jthread
#include <semaphore>		// Semaphores
#include <latch>			// Latches
#include <barrier>			// Barriers
#include <source_location>	// Supplies means to obtain source code location

// C++23 
#include <print>			// Formatted output library including std::print
#include <spanstream>		// std::basic_spanstream, std::basic_ispanstream, std::basic_ospanstream class templates and typedefs
#include <generator>		// std::generator class template
#include <mdspan>			// std::mdspan view
#include <flat_map>			// std::flat_map and std::flat_multimap container adaptors
#include <flat_set>			// std::flat_set and std::flat_multiset container adaptors
#include <stacktrace>		// Stacktrace library
#include <stdfloat>			// Optional extended floating - point types
#include <expected>			// std::expected class template

int main()
{
}